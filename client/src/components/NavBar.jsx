import React from "react";
import { Link, useParams } from "react-router-dom";

const Menu = () => {

    // const {id} = useParams()

  return (
    <div>
      <h1>lilMashriq</h1>
     {/* <nav>
        <div className="App">
          <nav>
            <Link to="/home">Home</Link>
          </nav>
          <nav>
            <Link to="/makecomment">Make Comment</Link>
          </nav>
          <nav>
            <Link to="/">Comments</Link>
          </nav>
           <nav>
            <Link to={`home/${id}`}>biraz bekle</Link>
          </nav> 
        </div>
      </nav>  */}
    </div>
  );
};

export default Menu;
